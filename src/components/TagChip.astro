---
const { nameOfTag, selected } = Astro.props;
---

{
    selected ? (
        <a class="tag tag-selected" href={`/blog/tag/${nameOfTag}`}>
            {nameOfTag}
        </a>
    ) : (
        <a class="tag tag-selectable" href={`/blog/tag/${nameOfTag}`}>
            {nameOfTag}
        </a>
    )
}



<style>
.tag {
    color: var(--text-col-pil);
    margin: 0 1px 0 0;
    padding: 2px 5px 3px 5px;
    font-size: calc(var(--text-size) * .9);
    background-color: var(--bg-col-pil);
    border-radius: 4px;
    text-decoration: none;
 }
  
 .tag-selected {
    background-color: var(--brand);
    color: var(--text-col-pil);
  }
.tag-selectable:hover {
  background-color: var(--soft-brand);
}
</style>